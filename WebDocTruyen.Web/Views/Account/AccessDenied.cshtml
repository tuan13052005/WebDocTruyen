@{
    ViewData["Title"] = "Không có quyền truy cập";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5 text-center" style="max-width:500px; margin:auto;">
    <div class="card shadow-sm border-danger">
        <div class="card-body p-5">
            <div style="font-size:4rem;">🚫</div>
            <h2 class="mt-3 text-danger">Không có quyền truy cập</h2>
            <p class="text-muted mt-3">
                Bạn không có quyền xem trang này.<br />
                Vui lòng đăng nhập bằng tài khoản có quyền phù hợp.
            </p>
            <div class="mt-4 d-flex gap-2 justify-content-center">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary">🏠 Về trang chủ</a>
                <a asp-controller="Account" asp-action="Login" class="btn btn-outline-secondary">🔑 Đăng nhập</a>
            </div>
        </div>
    </div>
</div>
EOF

# ── LỖI 8: Register.cshtml - thêm AntiForgeryToken
cat "$OUT/Views/Account/Register.cshtml" | head -5
echo "---check register---"
grep -n "AntiForgery\|antiforgery\|__RequestVerification" "$OUT/Views/Account/Register.cshtml" || echo "MISSING AntiForgeryToken!"
Output

﻿@{
    ViewData["Title"] = "Đăng ký";
    Layout = "~/Views/Shared/_Layout.cshtml";
}